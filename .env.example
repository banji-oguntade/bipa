# Unused in BIPA-S01 (scaffold only). Later stories will read these.
# Copy to `.env.local` when you need them. Do not commit real secrets.

# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/prepai"
# OPENAI_API_KEY=
